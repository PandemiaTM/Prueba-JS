var nombre = 'miguellariosriuz', apellido = 'larios'

var nombreMY = nombre.toUpperCase()
var apellidoML = apellido.toLocaleLowerCase()

var primeraLetra = apellido.charAt(0)
var cantidadDeLetras = nombre.length

var nombreCompleto = `${nombre} ${apellido.toUpperCase()}`

var str = nombre.substr(5,4)